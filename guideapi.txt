🚀 CONFIGURATION GEMINI API POUR IDE IA (MedAssist Pro)
=====================================================

Ce fichier sert de guide contextuel pour votre assistant IA (Cursor, Copilot, etc.). 
Il définit comment manipuler l'API sans casser l'architecture du projet.

1. ACCÈS À LA CLÉ API
---------------------
• Localhost : La clé doit être définie dans votre environnement système ou fichier .env sous le nom `API_KEY`.
• Production : Configurer une variable d'environnement `API_KEY` sur le serveur.
• ACCÈS CODE : Utilisez TOUJOURS `process.env.API_KEY`. Ne jamais créer de champ de saisie UI pour la clé.

2. INITIALISATION DU SDK
------------------------
Utilisez exclusivement le format suivant :
```ts
import { GoogleGenAI } from "@google/genai";
const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });
```

3. SÉLECTION DES MODÈLES
------------------------
• Tâches Rapides (Scribe, Nettoyage) : 'gemini-3-flash-preview'
• Raisonnement Médical / Analyse Dossier : 'gemini-3-pro-preview'
• Recherche Scientifique : 'gemini-3-pro-preview' avec tool `googleSearch`

4. CONFIGURATION RECOMMANDÉE
----------------------------
• Médical Expert : temperature: 0.1, thinkingConfig: { thinkingBudget: 32768 }
• Extraction Scribe : temperature: 0.2

5. RÈGLES DE SÉCURITÉ (RGPD/HDS)
--------------------------------
• Avant tout appel API : Passer les données par `utils/anonymizer.ts`.
• Ne jamais envoyer de noms réels, numéros de téléphone ou adresses mails aux serveurs de Google.

6. RÉSOLUTION D'ERREUR
----------------------
Si l'erreur "API_KEY not found" survient :
- Vérifiez l'injection de la variable d'environnement au build.
- Ne tentez pas de hardcoder une clé par défaut.