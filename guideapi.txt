🚀 GUIDE D'INTÉGRATION GEMINI API - MedAssist Pro
==================================================

Ce guide détaille les étapes pour configurer vos clés d'accès en environnement de développement (Local) et en production (Professionnel).

---

1. OBTENTION DE LA CLÉ (API KEY)
--------------------------------
1. Rendez-vous sur Google AI Studio (https://aistudio.google.com/).
2. Connectez-vous avec votre compte professionnel.
3. Cliquez sur "Get API Key" puis "Create API Key in new project".
4. Copiez précieusement cette clé. Elle est confidentielle.

2. CONFIGURATION EN LOCAL (DÉVELOPPEMENT)
-----------------------------------------
Le code de MedAssist-IA Pro utilise exclusivement `process.env.API_KEY`.

Étapes pour votre IDE (VS Code, Cursor, etc.) :
1. Créez un fichier nommé `.env` à la racine de votre projet (si vous utilisez Vite/Webpack).
2. Ajoutez la ligne suivante :
   VITE_API_KEY=votre_cle_ici
   (Note: Si vous utilisez un environnement Node pur, utilisez API_KEY=votre_cle_ici)

3. Redémarrez votre serveur de développement :
   `npm run dev` ou `vite`

Vérification :
Dans la console de votre navigateur, tapez : `console.log(import.meta.env.VITE_API_KEY)` pour vérifier que la clé est bien chargée.

3. DÉPLOIEMENT PROFESSIONNEL (PRODUCTION)
-----------------------------------------
NE JAMAIS VERSIONNER VOTRE CLÉ API (ne pas l'ajouter sur GitHub).

Étapes de déploiement (Vercel, Netlify, Cloudflare, AWS) :
1. Accédez au tableau de bord de votre hébergeur.
2. Allez dans "Settings" ou "Environment Variables".
3. Ajoutez une nouvelle variable :
   - Key : API_KEY
   - Value : [Votre Clé API réelle]
4. Déployez. L'infrastructure injectera automatiquement la clé lors de la compilation.

4. RÈGLES DE DÉVELOPPEMENT (SDK @google/genai)
----------------------------------------------
Pour toute modification du code, respectez scrupuleusement ces standards :

Initialisation :
   const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });

Appel Modèle (Génération de texte) :
   const response = await ai.models.generateContent({
     model: 'gemini-3-flash-preview',
     contents: 'Votre prompt ici',
   });

Appel Modèle (Recherche Grounding - Pro) :
   // Utiliser gemini-3-pro-preview pour les recherches web
   const response = await ai.models.generateContent({
     model: 'gemini-3-pro-preview',
     contents: prompt,
     config: { tools: [{ googleSearch: {} }] }
   });

5. RÉSOLUTION DES PROBLÈMES (TROUBLESHOOTING)
---------------------------------------------
• Page noire au chargement : Souvent dû à une erreur de syntaxe dans `.env` ou une clé invalide. Vérifiez la console (F12).
• Erreur 403 (Forbidden) : Votre clé n'a pas les droits pour le modèle demandé (ex: Gemini 3 nécessite parfois d'être en mode "Pay-as-you-go").
• Sécurité Patient : Le code MedAssist anonymise les données via `utils/anonymizer.ts` avant l'envoi. Ne désactivez jamais ce module.

---
Expertise Senior : Privilégiez toujours l'usage des variables d'environnement système pour garantir l'étanchéité des données de santé.