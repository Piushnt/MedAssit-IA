
# 🩺 MedAssist-IA - Guide d'Installation Local (VS Code)

Ce guide vous explique comment lancer l'application sur votre ordinateur après avoir cloné le dépôt.

## 🛠️ Pré-requis
1. **Node.js** installé sur votre machine (téléchargez sur nodejs.org).
2. **VS Code** comme éditeur.

## 🚀 Étapes pour visualiser le projet

1. **Ouvrez le terminal dans VS Code** (`Ctrl + J` ou `Cmd + J`).
2. **Installez les outils nécessaires** (une seule fois) :
   ```bash
   npm install
   ```
3. **Configurez votre clé API** :
   Créez un fichier nommé `.env` à la racine du projet et ajoutez votre clé dedans :
   ```text
   VITE_GEMINI_API_KEY=votre_cle_ici
   ```
   *(Note : Pour ce projet spécifique, l'application cherche `process.env.API_KEY`. Si vous utilisez Vite, lancez avec la commande ci-dessous).*

4. **Lancez le serveur de développement** :
   ```bash
   npm run dev
   ```
5. **Ouvrez votre navigateur** à l'adresse qui s'affiche (généralement `http://localhost:5173`).

## 📁 Structure et Développement
- `App.tsx` : C'est ici que vous modifiez la logique principale.
- `index.html` : Structure de base et importations via ESM.sh.
- `services/geminiService.ts` : Gère les appels à l'IA.

## 🔑 Note sur la Clé API
Votre clé `AIzaSyCMz6...` est maintenant prête. Pour qu'elle fonctionne localement, assurez-vous de l'injecter dans votre environnement de terminal ou d'utiliser un outil comme Vite qui gère les fichiers `.env`.

---
Dernière mise à jour : Guide local ajouté.
